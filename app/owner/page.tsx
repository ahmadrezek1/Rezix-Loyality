import { requireAdmin } from '@/lib/auth'; import { getDashboardData } from '@/lib/store'; import { redirect } from 'next/navigation'; import { Scissors,Gift,Users,History,UserPlus,ShieldCheck } from 'lucide-react';
export const dynamic='force-dynamic';
export default async function OwnerPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){await requireAdmin(); const db=await getDashboardData(); if(!db.business) redirect('/setup'); const q=await searchParams; return <main><header className="topbar"><div className="brand"><img src="/icon.svg" alt="Rezix"/><div><b>REZIX</b><span>OWNER</span></div></div><form method="post" action="/api/logout"><button type="submit" className="secondary">Abmelden</button></form></header><div className="shell"><div className="page-title"><div><span className="eyebrow">BUSINESS OVERVIEW</span><h1>{db.business.name}</h1><p>Live-Daten aus PostgreSQL. Keine Demo-Kunden und keine künstlichen Kennzahlen.</p></div><div className="pill"><ShieldCheck size={15}/> Admin</div></div><div className="metrics"><Metric label="Besuche heute" value={String(db.visitsToday)} icon={<Scissors/>}/><Metric label="Kunden" value={String(db.totalCustomers)} icon={<Users/>}/><Metric label="Teamkonten" value={String(db.staffCount)} icon={<UserPlus/>}/><Metric label="Belohnungsziel" value={String(db.business.rewardTarget)} icon={<Gift/>}/></div><div className="owner-grid"><section className="panel"><h2>Teamzugang erstellen</h2><p className="muted">Nur hier angelegte Mitarbeiter können den Staff-Bereich öffnen und Stempel vergeben.</p>{q.staffAdded&&<div className="success">Mitarbeiter wurde erstellt.</div>}{q.staffError&&<div className="alert">Bitte gültige Daten und mindestens 8 Zeichen als Passwort eingeben.</div>}{q.staffExists&&<div className="alert">Diese E-Mail-Adresse existiert bereits.</div>}<form className="stack-form" method="post" action="/api/staff/create"><label>Name</label><input name="name" required/><label>E-Mail</label><input name="email" type="email" required/><label>Temporäres Passwort</label><input name="password" type="password" minLength={8} required/><label>Rolle</label><select name="role"><option value="staff">Mitarbeiter</option><option value="manager">Manager</option></select><button className="gold" type="submit">Zugang erstellen</button></form></section><section className="panel"><h2>Kunden</h2>{db.customers.length===0?<div className="empty-block"><Users size={36}/><b>Noch keine Kunden</b><span>Die Liste bleibt leer, bis sich echte Kunden registrieren.</span></div>:<div className="attention">{db.customers.map(c=><div className="row" key={c.id}><div className="avatar sm">{c.name.slice(0,1).toUpperCase()}</div><div><b>{c.name}</b><span>{c.code} · {c.stamps}/{db.business!.rewardTarget} Stempel</span></div></div>)}</div>}</section></div><section className="panel quick"><h2>Aktuelle Konfiguration</h2><div className="quick-grid"><Info icon={<Gift/>} title="Belohnung" text={db.business.rewardText}/><Info icon={<Scissors/>} title="Ziel" text={`${db.business.rewardTarget} Stempel`}/><Info icon={<History/>} title="Gespeicherte Besuche" text={String(db.visitCount)}/></div></section></div></main>}
function Metric({label,value,icon}:{label:string,value:string,icon:React.ReactNode}){return <div className="metric"><div className="iconbox">{icon}</div><span>{label}</span><b>{value}</b><small>Live</small></div>}
function Info({icon,title,text}:{icon:React.ReactNode,title:string,text:string}){return <div className="info"><div className="iconbox">{icon}</div><div><b>{title}</b><span>{text}</span></div></div>}

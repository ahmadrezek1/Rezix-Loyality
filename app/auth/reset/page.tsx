export default async function Reset({searchParams}:{searchParams:Promise<{token?:string;error?:string;done?:string}>}){const q=await searchParams;if(q.done)return <main><section className="join"><div className="join-card"><img src="/icon.svg" alt="Rezix"/><h1>Passwort aktualisiert</h1><p>Dein Passwort wurde geändert. Alle bestehenden Sitzungen wurden beendet.</p><a className="gold full link-button" href="/">Zur Anmeldung</a></div></section></main>;return <main><section className="join"><form className="join-card" method="post" action="/api/auth/reset"><img src="/icon.svg" alt="Rezix"/><span className="eyebrow">SICHERHEIT</span><h1>Neues Passwort</h1>{q.error&&<div className="alert">Der Link ist ungültig/abgelaufen oder das Passwort erfüllt die Anforderungen nicht.</div>}<input type="hidden" name="token" value={q.token||''}/><label>Neues Passwort</label><input name="password" type="password" minLength={12} autoComplete="new-password" required/><label>Passwort wiederholen</label><input name="confirm" type="password" minLength={12} autoComplete="new-password" required/><small>Mindestens 12 Zeichen. Verwende kein Passwort aus einem anderen Dienst.</small><button className="gold full" type="submit">Passwort speichern</button></form></section></main>}

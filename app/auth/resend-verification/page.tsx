export default async function Resend({searchParams}:{searchParams:Promise<{role?:string;sent?:string}>}){const q=await searchParams;const role=q.role==='friseur'?'friseur':'manager';return <main><section className="join"><form className="join-card" method="post" action="/api/auth/resend-verification"><img src="/icon.svg" alt="Rezix"/><span className="eyebrow">E-MAIL BESTÄTIGUNG</span><h1>Bestätigungslink erneut senden</h1><p>Wenn ein unbestätigtes Konto existiert, senden wir einen neuen Link.</p>{q.sent&&<div className="success">Wenn ein passendes Konto existiert, wurde eine E-Mail versendet.</div>}<input type="hidden" name="role" value={role}/><label>E-Mail-Adresse</label><input name="email" type="email" required/><button className="gold full" type="submit">Link senden</button></form></section></main>}

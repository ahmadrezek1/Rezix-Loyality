export default async function Forgot({searchParams}:{searchParams:Promise<{role?:string;sent?:string}>}){const q=await searchParams;const role=q.role==='friseur'?'friseur':'manager';return <main><section className="join"><form className="join-card" method="post" action="/api/auth/forgot"><img src="/rezix-logo.svg" alt="Rezix"/><span className="eyebrow">KONTOZUGANG</span><h1>Passwort vergessen</h1><p>Gib deine registrierte E-Mail-Adresse ein. Wenn ein passendes Konto existiert, senden wir dir einen sicheren Link.</p>{q.sent&&<div className="success">Wenn ein Konto existiert, wurde eine E-Mail versendet.</div>}<input type="hidden" name="role" value={role}/><label>E-Mail-Adresse</label><input name="email" type="email" required autoComplete="email"/><button className="gold full" type="submit">Reset-Link anfordern</button><a className="text-link" href={role==='manager'?'/manager/login':'/friseur/login'}>Zurück zur Anmeldung</a></form></section></main>}

import { requireAdmin } from '@/lib/auth'; import { getBusiness } from '@/lib/store'; import { redirect } from 'next/navigation';
export const dynamic='force-dynamic';
export default async function Setup({searchParams}:{searchParams:Promise<{error?:string}>}){ await requireAdmin(); if(await getBusiness()) redirect('/owner'); const q=await searchParams; return <main><header className="topbar"><div className="brand"><img src="/icon.svg" alt="Rezix"/><div><b>REZIX</b><span>FIRST SETUP</span></div></div></header><section className="join"><form className="join-card wide" method="post" action="/api/setup"><span className="eyebrow">ERSTEINRICHTUNG</span><h1>Rezix Loyalty einrichten</h1><p>Es sind noch keine Kunden oder Mitarbeiter angelegt. Richte zuerst dein Geschäft und die Belohnungsregel ein.</p>{q.error&&<div className="alert">Bitte alle Felder korrekt ausfüllen.</div>}<label>Name des Geschäfts</label><input name="name" required/><label>Stempel bis zur Belohnung</label><input name="rewardTarget" type="number" min="2" max="30" defaultValue="10" required/><label>Belohnung</label><input name="rewardText" required/><button className="gold full" type="submit">Einrichtung abschließen</button></form></section></main> }

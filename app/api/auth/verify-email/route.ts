import { NextResponse } from 'next/server';import { consumeOneTimeToken } from '@/lib/auth-store';import { markStaffEmailVerified } from '@/lib/store';import { audit } from '@/lib/security';
export async function GET(req:Request){const token=new URL(req.url).searchParams.get('token')||'';const t=await consumeOneTimeToken(token,'email_verify');if(!t)return NextResponse.redirect(new URL('/?verifyError=1',req.url),303);await markStaffEmailVerified(t.actor_id);await audit({actorType:t.actor_type,actorId:t.actor_id,action:'auth.email_verified',req}).catch(()=>{});return NextResponse.redirect(new URL(`/${t.actor_type}/login?verified=1`,req.url),303)}

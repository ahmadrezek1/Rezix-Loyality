import { SALON_PUBLIC_ORIGIN } from '@/lib/salon-links';
import Link from 'next/link';
import type { Business } from '@/lib/store';
import { salonManagers } from '@/lib/admin-business';
export default async function SalonAdministration({business:b}:{business:Business}){
 const managers=await salonManagers(b.id);
 return <section className="panel"><h2>Salon bearbeiten</h2><p className="muted">Die öffentliche Adresse bleibt unverändert, damit bestehende QR-Codes weiter funktionieren.</p><form className="stack-form" method="post" action="/api/admin/business/update"><input type="hidden" name="businessId" value={b.id}/><input type="hidden" name="action" value="edit"/><label htmlFor="salon-name">Salonname</label><input id="salon-name" name="name" defaultValue={b.name} maxLength={120} required/><button className="gold">Speichern</button></form><h3>Salonstatus</h3><p className="muted">Sperren und Archivieren stoppen das Treueprogramm. Daten bleiben erhalten. Ein bestehendes Abonnement wird dadurch nicht gekündigt.</p><form className="page-actions" method="post" action="/api/admin/business/update"><input type="hidden" name="businessId" value={b.id}/>{!b.active?<button className="secondary" name="action" value="activate">Salon aktivieren</button>:<button className="secondary" name="action" value="suspend">Salon sperren</button>}{!b.archivedAt&&<button className="secondary" name="action" value="archive">Salon archivieren</button>}</form><h3>Tarif manuell zuweisen</h3><p className="muted">Für Vereinbarungen ohne laufendes Online-Abonnement. Diese Zuweisung aktiviert den Tarif und löst keine Zahlung aus. Bestehende Abonnements werden im Zahlungsportal verwaltet.</p><form className="stack-form" method="post" action="/api/admin/business/update"><input type="hidden" name="businessId" value={b.id}/><input type="hidden" name="action" value="plan"/><label htmlFor="manual-plan">Tarif</label><select id="manual-plan" name="plan" defaultValue={b.billingPlan==='trial'?'starter':b.billingPlan}><option value="starter">Starter · 1 Friseur</option><option value="professional">Professional · 5 Friseure</option><option value="business">Business · 100 Friseure</option></select><button className="secondary" disabled={!!b.stripeSubscriptionId&&!['canceled','incomplete_expired'].includes(b.subscriptionStatus)}>Tarif zuweisen</button></form><h3>Manager-Zugang</h3>{managers.map(m=><div className="row" key={m.id}><div><b>{m.name}</b><span>{m.email} · {m.email_verified_at?'Bestätigt':'Bestätigung ausstehend'}</span></div><form method="post" action="/api/admin/business/reset-manager"><input type="hidden" name="businessId" value={b.id}/><input type="hidden" name="managerId" value={m.id}/><button className="secondary" disabled={!m.active}>{m.email_verified_at?'Passwort-Link senden':'Bestätigungslink senden'}</button></form></div>)}<details className="metadata-details"><summary>Betriebsinformationen</summary><dl><dt>Betriebs-ID</dt><dd>{b.id}</dd><dt>Erstellt</dt><dd>{new Date(b.createdAt).toLocaleDateString('de-AT')}</dd><dt>Öffentliche Karte</dt><dd><Link href={`${SALON_PUBLIC_ORIGIN}/s/${encodeURIComponent(b.slug)}`} target="_blank" rel="noopener noreferrer">{SALON_PUBLIC_ORIGIN}/s/{b.slug}</Link></dd><dt>Status</dt><dd>{b.archivedAt?'Archiviert':b.active?'Aktiv':'Gesperrt'}</dd></dl></details></section>
}
